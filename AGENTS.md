You must only work on the `client_v2` directory when working on the frontend if not specified otherwise. However, you can read files from the `client` directory if needed.

Follow coding best practices and maintain a clean codebase.
Ensure you write modular code and avoid duplication without adding extra complexity.
Write minimal code to achieve the desired functionality.

**_ You MUST not touch file outside the root directory of the project. _**


# Commit & Pull Request Guidelines
History favors short, domain-first summaries (shop api integrations, reset migrations). Keep commits scoped, imperative, and mention the touched module (server, web, infra). PRs should include a description, linked issue/context, migration or seed notes, UI evidence (screenshot/gif), and confirmation that docker compose up plus both dev servers succeeded along with relevant test output.#